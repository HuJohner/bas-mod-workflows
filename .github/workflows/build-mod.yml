name: Build Mod Addressables (Reusable)

on:
  workflow_call:
    secrets:
      UNITY_USERNAME:
        required: true
      UNITY_PASSWORD:
        required: true

env:
  BASSDK_PATH: BasSDK
  UNITY_VERSION: '2021.3.38f1'

jobs:
  build:
    name: Build (${{ matrix.target.name }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        target:
          - name: Windows
            method: CIBuildAddressables.BuildWindows
            build_target: StandaloneWindows64
            artifact: mod-windows
          - name: Android
            method: CIBuildAddressables.BuildAndroid
            build_target: Android
            artifact: mod-android

    steps:
      # -----------------------------------------------------------------------
      # 1. Check out the shared workflow repo so we can access scripts/
      # -----------------------------------------------------------------------
      - name: Checkout workflow repo (for CI scripts)
        uses: actions/checkout@v4
        with:
          repository: HuJohner/bas-mod-workflows
          path: bas-mod-workflows

      # -----------------------------------------------------------------------
      # 2. Check out the calling mod repo
      # -----------------------------------------------------------------------
      - name: Checkout mod repo
        uses: actions/checkout@v4
        with:
          repository: ${{ github.repository }}
          ref: ${{ github.ref }}
          path: mod

      # -----------------------------------------------------------------------
      # 3. Free up disk space before any large downloads
      # -----------------------------------------------------------------------
      - name: Free disk space
        uses: jlumbroso/free-disk-space@v1.3.1
        with:
          tool-cache: false   # Keep tool cache — unity-setup uses it
          android: true
          dotnet: true
          haskell: true
          large-packages: true
          docker-images: true
          swap-storage: false  # Keep swap — helps with memory-starved imports

      # -----------------------------------------------------------------------
      # 4. Restore cached BasSDK clone (avoids ~2 min clone + full reimport)
      # -----------------------------------------------------------------------
      - name: Cache BasSDK
        id: cache-bassdk
        uses: actions/cache@v4
        with:
          path: ${{ env.BASSDK_PATH }}
          key: BasSDK-${{ env.UNITY_VERSION }}-${{ matrix.target.name }}
          # No restore-keys — BasSDK cache must be exact version match

      - name: Clone BasSDK
        if: steps.cache-bassdk.outputs.cache-hit != 'true'
        run: |
          git clone --depth 1 \
            https://dev.azure.com/Warpfrog/BasSDK/_git/BasSDK \
            ${{ env.BASSDK_PATH }}

      # -----------------------------------------------------------------------
      # 5. Restore cached Unity Library (avoids full reimport on cache hit)
      #    Must happen BEFORE Unity starts, so it doesn't reimport everything
      # -----------------------------------------------------------------------
      - name: Cache Unity Library
        uses: actions/cache@v4
        with:
          path: ${{ env.BASSDK_PATH }}/Library
          key: Library-${{ matrix.target.name }}-${{ env.UNITY_VERSION }}-${{ hashFiles('mod/Assets/**') }}
          restore-keys: |
            Library-${{ matrix.target.name }}-${{ env.UNITY_VERSION }}-
            Library-${{ matrix.target.name }}-

      # -----------------------------------------------------------------------
      # 6. Link mod assets + inject CI script into BasSDK
      # -----------------------------------------------------------------------
      - name: Set up project structure
        run: |
          MOD_NAME="${{ github.event.repository.name }}"
          PERSONAL_DIR="${{ env.BASSDK_PATH }}/Assets/Personal"
          EDITOR_DIR="${{ env.BASSDK_PATH }}/Assets/Editor"

          mkdir -p "$PERSONAL_DIR"
          # Remove stale link from a previous cached run
          rm -rf "$PERSONAL_DIR/$MOD_NAME"
          ln -s "$(pwd)/mod/Assets" "$PERSONAL_DIR/$MOD_NAME"
          echo "Linked mod assets -> $PERSONAL_DIR/$MOD_NAME"

          mkdir -p "$EDITOR_DIR"
          cp bas-mod-workflows/scripts/CIBuildAddressables.cs "$EDITOR_DIR/CIBuildAddressables.cs"
          echo "Injected CIBuildAddressables.cs into $EDITOR_DIR"

      # -----------------------------------------------------------------------
      # 7. Install Unity (cached after first run)
      # -----------------------------------------------------------------------
      - name: Install Unity ${{ env.UNITY_VERSION }}
        uses: RageAgainstThePixel/unity-setup@v2
        with:
          version-file: 'none'
          unity-version: ${{ env.UNITY_VERSION }}
          build-targets: ${{ matrix.target.build_target }}
          cache-installation: true

      # -----------------------------------------------------------------------
      # 8. Activate Unity Personal license
      # -----------------------------------------------------------------------
      - name: Activate Unity license
        uses: RageAgainstThePixel/activate-unity-license@v2
        with:
          license: 'Personal'
          username: ${{ secrets.UNITY_USERNAME }}
          password: ${{ secrets.UNITY_PASSWORD }}

      # -----------------------------------------------------------------------
      # 9. Build addressables
      # -----------------------------------------------------------------------
      - name: Build addressables (${{ matrix.target.name }})
        uses: RageAgainstThePixel/unity-action@v2
        with:
          editor-path: ${{ env.UNITY_EDITOR_PATH }}
          project-path: ${{ env.BASSDK_PATH }}
          build-target: ${{ matrix.target.build_target }}
          log-name: ${{ matrix.target.name }}-addressables
          args: '-batchmode -quit -executeMethod ${{ matrix.target.method }}'

      # -----------------------------------------------------------------------
      # 10. Upload build output as artifacts
      # -----------------------------------------------------------------------
      - name: Upload artifacts (${{ matrix.target.name }})
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.target.artifact }}
          path: |
            ${{ env.BASSDK_PATH }}/BuildStaging/AddressableAssets/${{ matrix.target.name == 'Android' && 'Android' || 'Windows' }}/**
          if-no-files-found: warn
          retention-days: 30